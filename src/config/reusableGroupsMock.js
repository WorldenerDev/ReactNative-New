/**
 * Toggle mock data for reusable-groups (crew + per-trip participation).
 * Set to false for production — real APIs are wired in crewGroupsService.
 */
export const REUSABLE_GROUPS_MOCK_ENABLED = false;
